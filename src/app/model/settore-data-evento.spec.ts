import { SettoreDataEvento } from './settore-data-evento';

describe('SettoreDataEvento', () => {
  it('should create an instance', () => {
    expect(new SettoreDataEvento()).toBeTruthy();
  });
});
