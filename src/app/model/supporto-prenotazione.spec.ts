import { SupportoPrenotazione } from './supporto-prenotazione';

describe('SupportoPrenotazione', () => {
  it('should create an instance', () => {
    expect(new SupportoPrenotazione()).toBeTruthy();
  });
});
